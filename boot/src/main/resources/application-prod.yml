spring:
  jpa:
    database: mysql
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: wuxc
    password: hellowuxc
    url: jdbc:mysql://localhost:3306/employees?serverTimezone=Asia/Shanghai
    initialization-mode: always
#    schema:
#      - classpath:createAclSchemaMySQL.sql
#      - classpath:createUserSchema.sql
    data: classpath:data-mysql.sql